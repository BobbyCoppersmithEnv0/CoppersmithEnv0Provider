# Terraform provider configuration
# When running through env0, ENV0_API_KEY and ENV0_API_SECRET are
# automatically injected as environment variables — no explicit
# credentials needed in the provider block.
provider "env0" {}
